apiVersion: v1
kind: Service
metadata:
  name: express-svc
  labels:
    app: express-k8s-app
spec:
  selector:
    app: express-k8s-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: http
      nodePort: 30000
  type: NodePort